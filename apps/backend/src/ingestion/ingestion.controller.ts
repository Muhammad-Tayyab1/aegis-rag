import { BadRequestException, Controller, Post, UploadedFile, UseGuards, UseInterceptors } from '@nestjs/common';import{FileInterceptor}from'@nestjs/platform-express';import{AuthGuard}from'@nestjs/passport';import pdf from 'pdf-parse';import mammoth from 'mammoth';import{CurrentUser}from'../common/tenant/current-user.decorator';import{AuthenticatedUser}from'../common/tenant/tenant.types';import{IngestionService}from'./ingestion.service';
@UseGuards(AuthGuard('jwt')) @Controller('documents') export class IngestionController{constructor(private s:IngestionService){} @Post('upload') @UseInterceptors(FileInterceptor('file',{limits:{fileSize:10_000_000}})) async upload(@UploadedFile()f:Express.Multer.File,@CurrentUser()u:AuthenticatedUser){if(!f)throw new BadRequestException('file required');const ext=f.originalname.toLowerCase().split('.').pop();let content:string;if(ext==='pdf')content=(await pdf(f.buffer)).text;else if(ext==='docx')content=(await mammoth.extractRawText({buffer:f.buffer})).value;else if(['txt','md','csv','json'].includes(ext??''))content=f.buffer.toString();else throw new BadRequestException('Supported formats: PDF, DOCX, TXT, MD, CSV, JSON');return this.s.ingest(u.tenantId,f.originalname,content,'file');}}
