import{Body,Controller,Post,UseGuards}from'@nestjs/common';import{AuthGuard}from'@nestjs/passport';import{IsString}from'class-validator';import{CurrentUser}from'../common/tenant/current-user.decorator';import{AuthenticatedUser}from'../common/tenant/tenant.types';import{PrismaService}from'../common/database/prisma.service';import{RetrievalService}from'../retrieval/retrieval.service';class Ask{ @IsString() question!:string }
@UseGuards(AuthGuard('jwt'))@Controller('chat')export class ChatController{constructor(private r:RetrievalService,private p:PrismaService){}@Post()async ask(@Body()b:Ask,@CurrentUser()u:AuthenticatedUser){const hits=await this.r.search(u.tenantId,b.question);const answer=hits.length?hits.slice(0,3).map(x=>x.content).join('\n\n').slice(0,3000):'I could not find relevant information in this workspace.';return this.p.withTenant(u.tenantId,async tx=>{const q=await tx.query.create({data:{tenantId:u.tenantId,userId:u.id,question:b.question,answer,tokensUsed:Math.ceil(answer.length/4)}});await tx.queryTrace.createMany({data:hits.map(h=>({tenantId:u.tenantId,queryId:q.id,stage:'fusion',chunkId:h.id,rank:h.rank,score:h.score,latencyMs:0,details:{filename:h.filename}}))});return{queryId:q.id,answer,citations:hits.map(h=>({chunkId:h.id,document:h.filename,snippet:h.content.slice(0,240)}))}})}}
